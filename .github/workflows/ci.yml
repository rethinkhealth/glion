name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize, reopened]

# Nothing here writes back: Codecov authenticates with its own secret and
# the pnpm cache needs only read scope.
permissions:
  contents: read

jobs:
  testing:
    timeout-minutes: 15
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
        node-version:
          - 22.x
          - 24.x

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build
        run: pnpm build
      - name: Testing
        run: pnpm test:coverage
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true

  linting:
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm ci:install
      - name: Linting
        run: pnpm check
      - name: Dependency version alignment
        run: pnpm syncpack
      - name: README standardization
        run: pnpm check:readmes

  check-types:
    name: Type checking
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build (declarations of workspace dependencies)
        run: pnpm build
      - name: Type checking (src and tests)
        run: pnpm check-types

  packaging:
    name: Packaging (publint)
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm ci:install
      - name: Packaging checks
        run: pnpm publint

  testing-bun:
    name: Testing (Bun runtime)
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build (all packages via turbo for correct dependency order)
        run: pnpm build
      - name: Run Bun-runtime tests across all packages that opt in
        run: pnpm test:bun

  testing-deno:
    name: Testing (Deno runtime)
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: 2.9.6
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build (all packages via turbo for correct dependency order)
        run: pnpm build
      - name: Run Deno-runtime tests across all packages that opt in
        run: pnpm test:deno

  testing-cf:
    name: Testing (Cloudflare Workers / workerd)
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build (all packages via turbo for correct dependency order)
        run: pnpm build
      - name: Run Workers-runtime tests across all packages that opt in
        run: pnpm test:cf
        env:
          NODE_OPTIONS: "--max_old_space_size=8192"

  testing-deno:
    name: Testing (Deno runtime)
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"
      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Install dependencies
        run: pnpm ci:install
      - name: Build (all packages via turbo for correct dependency order)
        run: pnpm build
      - name: Run Deno-runtime tests across all packages that opt in
        run: pnpm test:deno
