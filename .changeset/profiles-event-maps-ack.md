---
"@glion/profiles": patch
---

Every version's event map gains `ACK: "ACK"`, and v2.5 and v2.5.1 gain `QRY: "QRY"`, so every bundled structure maps to itself. v2.4's event map no longer maps `QRY_P04` to a structure v2.4 does not define.
